package com.project.questionandanswers.mappers;

import com.project.questionandanswers.application.model.PlayerDto;
import com.project.questionandanswers.infrastructure.entities.Player;
import org.junit.Assert;
import org.junit.jupiter.api.Test;
import org.mapstruct.factory.Mappers;

import java.time.LocalDate;


public class PlayerMapperTest {
/*
    private PlayerMapper mapper = Mappers.getMapper(PlayerMapper.class);

    @Test
    public void shouldMapPlayerToDto(){


        Player player = new Player(1L,"felipe","quiceno",LocalDate.now(),100);

        PlayerDto playerDto = mapper.playerToPlayerDto(player);

        System.out.println(playerDto);
        System.out.println(player);
        Assert.assertEquals(playerDto.getNamePlayer(),player.getName());
    }

*/

}
